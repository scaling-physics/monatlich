package com.monatlich

import org.junit.Assert.assertEquals
import org.junit.Test

/** Proves the unit-test toolchain works. Replaced by real tests in M2. */
class SanityTest {
    @Test
    fun arithmetic() {
        assertEquals(4, 2 + 2)
    }
}
